# ── Orchestrator ──
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# ── UI Layer ──
gradio>=4.0.0
# streamlit>=1.35.0  # alternative UI

# ── Local LLM ──
ollama>=0.4.0

# ── TTS ──
TTS>=0.22.0           # Coqui TTS / XTTS-v2
piper-tts>=1.2.0      # Piper (lightweight)

# ── Voice Clone ──
# openvoice            # install separately: pip install openvoice
# rvc-python           # install separately

# ── Avatar Renderer ──
# sadtalker            # clone from GitHub
# musetalk             # clone from GitHub

# ── Lip Sync ──
# wav2lip              # clone from GitHub

# ── Enhancement Pipeline ──
rembg>=2.0.50         # Background removal (MODNet/U2Net)
openai-whisper>=20231117  # Captions / transcription
faster-whisper>=1.0.0 # Faster captions
realesrgan>=0.3.0     # Upscaling
gfpgan>=1.3.8         # Face enhancement
pydub>=0.25.1         # Audio mixing
moviepy>=1.0.3        # Video compositing
Pillow>=10.0.0        # Image overlays / branding

# ── Output / Rendering ──
ffmpeg-python>=0.2.0  # FFmpeg wrapper

# ── Document Skills ──
python-docx>=1.1.0    # DOCX generation
reportlab>=4.0.0      # PDF generation
python-pptx>=0.6.22   # PPTX generation
openpyxl>=3.1.0       # XLSX reading

# ── File Reading ──
PyPDF2>=3.0.0         # PDF reading
python-multipart>=0.0.6  # File upload handling

# ── Data / Storage ──
sqlite-utils>=3.36    # SQLite helper
pydantic>=2.0.0       # Data validation

# ── Utilities ──
httpx>=0.27.0         # HTTP client (for API avatar providers)
aiofiles>=23.0.0      # Async file I/O
rich>=13.0.0          # CLI pretty output
typer>=0.12.0         # CLI framework
